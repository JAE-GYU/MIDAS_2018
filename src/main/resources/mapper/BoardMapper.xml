<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC
        "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.midasit.challenge.web.reservation.mapper.ReservationMapper">
    <select id="list" resultType="hashmap">
        select reservation_id, created_date, user_id, menu_id, group_id, state, comment
        from reservation
        order by reservation_id desc
    </select>
    <select id="view" parameterType="Reservation" resultType="Reservation">
        select idx, user_idx, user_nick, content, created, updated
        from t_board
        where idx=#{idx} and user_idx=#{user_idx}
    </select>
</mapper>